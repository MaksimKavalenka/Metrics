package by.training.editor;

public class StorageEditor {

    // NOT IMPLEMENTED
    // FOR THE FUTURE

    /*
     * private Transporter transporter;
     *
     * public StorageEditor() {
     *
     * }
     *
     * public static void runStorage() { for (TypeMetric type : TypeMetric.values()) {
     *
     * new Thread(new St()).start(); new St(); } }
     *
     * private class StorageExecutor implements Runnable {
     *
     * private TypeMetric type;
     *
     * public StorageExecutor(final TypeMetric type) { this.type = type; }
     *
     * @Override public void run() { Storage storage = new Storage(new Date(), Transporter.);
     * type.addToStorage(storage); }
     *
     * }
     */

}
